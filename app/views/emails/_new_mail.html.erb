<div class="card-body">
  <h4 class="card-title display-4">Nouvel email</h4>
  <%= simple_form_for(@email, html: { class: 'form-horizontal' }) do |f| %>
    <%= f.input :object %>
    <%= f.input :body %>
    <%= f.button :submit, class: "btn btn-info" %>
  <% end %>
</div>